{{#each postings as |post|}}
<section class="container">
    {{!-- <div class="card">
        <a href="/post/{{post.id}}">
            <div class="title">
                <h2 class="top-header" style="display: inline">{{post.title}}</h2>
                <h5 class="user">Posted by {{user.username}} on {{post.date_created}}</h5>
            </div>
            <div>
                <h4 class="text">{{post.content}}</h4>
            </div>
        </a>
    </div> --}}
    <div class="card text-center">
        <a href="/post/{{post.id}}">
            <div class="card-header">
                {{post.title}}
            </div>
            <div class="card-body">
                <p class="card-text">{{post.content}}</p>
            </div>
            <div class="card-footer text-muted">
                Posted by {{user.username}} on {{post.date_created}}
            </div>
        </a>
    </div>
</section>
{{/each}}